# Day96 — CFG branch/loop 복원

`ecx`와 `eax`는 0으로 초기화된다. `ecx < 4`인 동안 반복하며, `ecx`가 짝수일 때만 `eax += ecx`를 실행한다. 반복마다 `ecx`가 1씩 증가하고, `ecx == 4`가 되면 종료한다. 최종 `eax`는 2이다.

CFG에서 `B5 → B2`는 반복을 만드는 역방향 간선이고, `B2 → B6`은 반복문 종료 경로다. GDB에서는 `ecx`, `eax`, `RIP`를 관찰해 증가 과정과 짝수 분기, 종료 조건을 교차검증한다.
